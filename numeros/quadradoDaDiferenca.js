//Ler dois inteiros (variáveis A e B) e imprimir o resultado do quadrado da diferença do primeiro valor pelo segundo.
let a = Number(prompt("Qual o valor do primeiro número?"))
let b = Number(prompt("Qual o valor do segundo número?"))
let ab = (a -b)*(a-b)
alert("O quadrado da diferença de " +  a + " e " +  b + " é " + ab)
